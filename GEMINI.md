すべての応答は日本語にしてください

【AIの行動規範とマインドセット】

1. 的確なスコープ管理（オーバーエンジニアリングの禁止）
- 余計な深読みや、ユーザーが頼んでいない機能の追加は絶対に行わないこと。

2. 文脈と構造の把握（破壊的変更の防止）
- コードを書く前には、必ず「現在のシステムの構造」と「過去の会話の文脈」を振り返ること。
- 指示をそのまま実行した場合に、他の機能との矛盾やバグ（例：二重ログイン等）が起きないか、システム全体への影響を思考してから手を動かすこと。

3. セキュリティファースト（機密情報のハードコード禁止）
- APIキー、トークン、パスワードなどの機密情報は、絶対にプログラム内に直接書き込まないこと（ハードコード of 禁止）。
- 常に環境変数（OSの環境変数や `.env` ファイルなど）から読み込む設計にすること。
- **Gitプッシュやコミット等のコマンド実行前には、Gitのステージングに機密ファイルやコードのハードコードが含まれていないかを100%徹底してセルフチェック（確認）すること。**
- **APIキーや認証情報は環境変数に登録すれば解決するため、焦ってコードを直接修正して解決しようとせず、正しい手順を厳守すること。**

4. 段階的なアプローチと自己検証（賢く確実な問題解決）
- 複雑な問題に直面した際は、一度に全てを修正しようとせず、問題を小さく切り分けて一つずつ検証しながら進めること。
- エラーが発生した場合は、表面的なエラーを消すだけの絆創膏のような修正ではなく、「なぜそれが起きたのか（根本原因）」を分析して解決すること。
- 無関係な既存のコードやコメントを勝手に消したり変更したりせず、既存の意図を尊重すること。

5. 既存前提の批判的検証と自己検証（無条件受け入れの禁止）
- compaction（文脈圧縮）から復帰した際のサマリーや、既存コード内の「デモ仕様」「一時的」「対象外」といった記述を無条件の確定仕様として盲信しないこと。
- 常に「システム全体の一貫性（整合性）」および「セキュリティ（特に認証や管理権限）の妥当性」の観点から、本当に例外扱いのままで良いのか自発的に再検証すること。
- 特定の機能を実装スコープから除外、またはダミーで十分と判断する際は、明確かつ論理的な技術的根拠を自身で検証・説明すること。根拠が乏しい場合は、他のコンポーネントと動作仕様を統一する実装を選択すること。

6. 自律的思考を強制する「思考プロセス（Chain of Thought）」のルール化
コードの修正案を提示する前に、必ず以下の「思考の5ステップ」を踏んで、現状分析、批判的検証、影響評価、最適解の選定を徹底的に検証し、エラーの根本排除に努めること。
- **現状分析 (Context Check)**: 以前のルールや設定、エンドポイントの現状を正しく把握する。
- **批判的検証 (Critical Review)**: 以前のルールをそのまま適用した場合、新たなバグやエラーの見落とし（スルー）が発生しないか？「あえてルールを疑う」視点で検証する。
- **影響評価 (Impact Assessment)**: この修正が、バックグラウンドの処理や他のエンドポイント、システム全体に与える副作用（サイドエフェクト）を網羅的に洗い出す。
- **最適解の選定 (Optimization)**: 「過去のルールの維持」と「エラーの完全排除」が衝突した場合、後者（システムの正常動作）を最優先し、ルール側の変更・拡張案を提案する。

7. AIの優先順位（プライオリティ）の再定義
ルール同士や指示との競合が発生した場合、以下の優先順位に従って自律的に判断すること。
- **優先度 High (最優先)**: システムがエラーなく、期待通りに動作すること（例外処理やエンドポイントの整合性）。
- **優先度 Medium**: 変更による影響範囲（バックグラウンド処理等）が最小限であること、または影響が予測可能であること。
- **優先度 Low (最下位)**: 以前に設定されたローカルルールや記述パターンの維持。
※ 過去のルールを守るためにエラーを無視（スルー）することは**「最大の致命的バグ」**とみなす。

---

# RetailMedia System - バックエンド開発ルール (Backend Rules)

## 🚨 必須ルール1: データ永続化 (S3保存) の徹底

機能追加や既存機能の改善において、バックエンド（`server_retail_dist.js` 等）でデータを変更するエンドポイント（POST, PUT, DELETE）を作成・修正する場合は、**必ずS3への保存関数を呼び出すこと**を必須とします。

メモリ上の変数を更新しただけでは、システム再起動時（AppRunner等のコンテナ再起動）に変更内容がすべて消失し、致命的なデータロストに繋がります。

### 保存関数の使い分け

エンドポイントで扱うデータの種類に応じて、以下のいずれかの関数を必ず同期的に呼び出してください。

#### 1. `saveDatabase()`
- **対象データ**: ユーザー情報 (`users`)、サイネージ設定 (`storeData`, `signageState`)、キャンペーン情報 (`campaigns`)、実績データ (`transactions`) など、システム全般のデータ。
- **使用例**:
  ```javascript
  app.post('/api/example', (req, res) => {
      users[req.body.email] = req.body.data;
      if (typeof saveDatabase === 'function') saveDatabase(); // 必須
      res.json({ success: true });
  });
  ```

#### 2. `saveFinanceDB()`
- **対象データ**: 出金リクエスト (`withdrawalRequests`)、本人確認データ (`kycRequests`)、振込先口座情報 (`creatorBanks`)、代理店データ (`agencyReferrals`) などの金融・審査系データ。
- **使用例**:
  ```javascript
  app.post('/api/creator/withdraw', (req, res) => {
      withdrawalRequests.push(req.body.request);
      if (typeof saveFinanceDB === 'function') saveFinanceDB(); // 必須
      res.json({ success: true });
  });
  ```

---

## 🚨 必須ルール2: 認証とセキュリティ (Authentication & Security)

ユーザーの認証状態や機密情報を扱う際は、以下のセキュリティ基準を遵守してください。

1. **パスワードの平文保存禁止**: 
   新規登録やパスワードリセット時、パスワードは必ず `crypto.scryptSync` 等を用いてハッシュ化してから保存すること。
2. **グローバル変数によるセッション管理の禁止**:
   `currentUser` のようなグローバル変数にログイン状態を保持すると、複数ユーザーからの同時リクエスト時にセッション情報が混同する（他人のアカウントにログインしてしまう）致命的なバグが発生します。
   **必ず JWT (JSON Web Token) と HTTP-Only Cookie を用いてリクエスト単位で認証状態を管理** してください。

---

## 🚨 必須ルール3: 重要処理の排他制御 (Mutex & Concurrency)

出金処理やポイント付与など、**二重実行されるとシステムや財務に致命的な影響を与えるAPI** を実装する際は、必ずメモリ上の排他制御（Mutex）を組み込んでください。

- **実装例**:
  ```javascript
  const processingTasks = new Set();
  
  app.post('/api/critical/task', (req, res) => {
      const taskId = req.body.taskId;
      if (processingTasks.has(taskId)) {
          return res.status(409).json({ error: "現在処理中です" }); // 連打防止
      }
      processingTasks.add(taskId);
      
      try {
          // ... 重要な処理 ...
      } finally {
          processingTasks.delete(taskId); // 確実なロック解除
      }
  });
  ```

---

## 🚨 必須ルール4: スケーラビリティとメモリ管理 (Scalability)

現在進行中の SQLite への移行フェーズにおけるルールです。

1. **巨大なインメモリ同期処理の回避**:
   `users` や `financeDB` などの全件データが入った辞書（オブジェクト）に対する同期的な巨大JSON変換は、イベントループをブロックしサーバーを停止させる原因となります。
2. **新規機能のデータベース設計**:
   今後新たに追加するデータモデルや大量 of トランザクションが予想される機能（出金履歴、ログデータなど）は、極力JSONではなく **SQLite (`db_sqlite.js`) を用いた非同期処理** で設計してください。
3. **JSONメモリ限界時のRDS完全移行推奨**:
   インメモリJSON同期処理（`database.json` やS3への同期ループなど）によるメモリ肥大化が限界に達しそうな場合、またはクラッシュのリスクが発生した場合には、インメモリ管理とJSON保存処理を速やかに完全廃止し、すべてのデータストアをRDS（PostgreSQL）またはSQLiteへ完全移行（直接SQLクエリによるテーブル参照・保存）することを開発チームやユーザーに強く推奨・提案してください。

---

## 🚨 必須ルール5: 外部API・LLM of 耐障害性とモデル of 動的フォールバック (LLM API & Resilience)

Google Geminiなどの外部生成AI APIを使用する際は、モデルの逆止やAPI仕様の変更によってシステムが突然停止（500エラー等）することを防ぐため、以下の設計基準を遵守してください。

1. **複数モデルによる自動フォールバックとリトライ機構の搭載**
   単一のモデル名（例: `gemini-2.5-flash`）を決め打ちで利用するのではない、モデル障害や廃止に備え、優先度の高い順に複数のモデル候補をリスト化し、エラー発生時に自動で切り替えて再試行するラッパー関数を必ず経由してAPIを呼び出すこと。
   - **優先度リストの例**: `['gemini-2.5-flash', 'gemini-1.5-flash', 'gemini-2.5-pro']`
2. **通信障害やキー未設定時のフォールバック（デモ動作）の保証**
   APIキーが設定されていない場合や、通信障害等によりすべてのモデルで応答が得られなかった場合でも、システム自体をクラッシュ（500エラー）させず、事前に用意された適切なデモ用（フォールバック）データを返却してフロントエンドの表示や最低限のサービス継続を保証すること。

---

## 🚨 必須ルール6: 開発中のファイル破損・タグ整合性チェック (File Integrity & Syntax Validation)

セッション切り替え時などのコンテキスト忘却（忘れること）によるバグ再発を防ぐため、HTML/JSファイルを編集またはGitプッシュする際は、以下のファイル整合性チェックを必ず実行してください。

1. **スクリプト・HTMLタグの「数」と「末尾」の整合性検証**
   - 単に正規表現で `<script>`〜`</script>` の間を切り出して文法チェックをかけるだけでは、**「閉じタグ自体が欠落してファイルが途中で切れている破損」** を検知できません。
   - ファイルを編集した際は、必ず `<script>`と`</script>`、`<form>`と`</form>`、`<div>`と`</div>` などの**開きタグと閉じタグの総数が一致していること**、およびファイル末尾が `</html>` などの正規のタグで閉じられているかをプログラム的または目視で必ず検証すること。
2. **Gitプッシュ前の構文チェック実行義務**
   - どのような軽微な変更であっても、Gitプッシュを行う直前には、プロジェクト内にある構文検証スクリプトを実行し、エラー（Syntax Error）が一切存在しないことを確認してからプッシュすること。

---

## 🚨 必須ルール7: ネイティブモジュールと実行環境の互換性確保および不要な依存の排除

1. **実行環境（GLIBC等）の差異によるネイティブバイナリクラッシュの防止**
   - `sqlite3` や `bcrypt` 等のC/C++ネイティブモジュールを伴うNode.jsパッケージは、本番環境（AWS App Runner等）のOS・GLIBCバージョンと、パッケージが要求するコンパイル済みバイナリの不一致により `ERR_DLOPEN_FAILED (GLIBC_X.XX not found)` を起こすリスクが極めて高い。
   - ネイティブモジュールに依存するパッケージを利用する場合、本番デプロイ時に実行環境のライブラリに合わせて確実にその場でネイティブビルドを強制する設定（例：`package.json` の `prestart` における `npm rebuild --build-from-source` の実行）を必ず仕込んでおくこと。
2. **本番環境とローカル開発環境の依存切り分け・クリーンロードの徹底**
   - 本番環境でPostgreSQLを利用する一方、ローカルでSQLiteを利用するなど、環境ごとにデータベース等の接続先を切り替える設計にする場合、本番で不要なはずのモジュール（例：`sqlite3`）を無条件に `require` させてサーバーを起動クラッシュさせないこと。
   - 使用しないデータベースモジュールは、環境変数などの実行条件に応じて動的（ダイナミック）にインポートするか、不要な依存関係をコードから完全に排除して本番環境の動作をクリーンに保つこと。

---

## 🚨 必須ルール8: AI向け開発・デプロイ検証ルール（周辺影響 of 徹底検証）

コードの生成、変更、またはデプロイ手順の適用を行う際は、単に「要求されたコードを書く（シングルタスクの完了）」ことだけに集中せず、その変更がシステム全体（バックエンド、データベース、Git構成、実行環境）に与える影響を多角的に検証し、副作用（バグ・クラッシュ）をゼロにする責任がある。
すべての開発・デプロイ作業において、以下の**「4つの検証レイヤー」**に基づく自己検証（セルフチェック）を実行すること。

### 1. データベース（DB）移行・スキーマ変更レイヤー
変更にDBの構造変更や移行が含まれる場合、以下を必ず検証する。
- **データ整合性:** 既存のデータが新しいスキーマで破損、または消失（Nullエラーや型不一致など）しないか。
- **ロールバックの担保:** 移行（Up）が失敗した場合、または本番で問題が発生した場合に、安全かつ即座に元の状態に戻す手順（Down）やフォールバックが確立されているか。
- **N+1問題・パフォーマンス:** インデックスの再構築や、クエリの書き換えによってバックエンドの処理速度が極端に低下しないか。

### 2. コード品質・構文・ランタイムエラー防止レイヤー
- **静的解析・構文チェック:** 記述したコードにシンタックスエラー（構文エラー）、タイポ、インポート漏れ（未定義の変数・関数の参照）がないか。
- **依存関係の衝突:** 新しいライブラリやパッケージを追加・更新した場合、既存の依存関係（package.json 等）と衝突しないか、また実行環境（AWSコンテナ等）でネイティブバイナリの互換性エラーを起こさないか。
- **エラーハンドリング:** バックエンド処理で例外が発生した場合に、システム全体がクラッシュせず、適切にキャッチしてエラーレスポンスとログを吐き出す構造になっているか。

### 3. Gitプッシュ・コンフリクト回避レイヤー
- **影響範囲の最小化:** 変更対象外のファイルや、不要な設定ファイル、機密情報（環境変数など）を誤ってコミットやステージングに含めていないか。
- **コンフリクト回避:** ターゲットブランチへマージ・デプロイする際、コンフリクトが発生する可能性のある既存コード（特に共通関数や共通モジュール）はないか。

### 4. 周辺機能への影響（サイドエフェクト）検証レイヤー
- **結合バグの予測:** 今回修正した項目（API、関数、コンポーネント）を呼び出している「他の機能」や「フロントエンド/バックエンドの通信」が壊れないか。
- **状態管理の整合性:** グローバルな状態（State）やセッション、キャッシュの仕様変更により、別画面や別処理で予期せぬ挙動が発生しないか。

---

## 🚨 必須ルール9: ローカル同期除外とデプロイの仕様

コンテキストの切り替えや会話の圧縮に伴うAIの忘却を防ぐため、以下の最新の開発ルールと認証仕様を厳守してください。

### 1. デスクトップのローカルフォルダへの同期【除外】
- **開発ワークスペースでのコード修正を、デスクトップのローカルプレビューフォルダ (`C:\Users\one\Desktop\RetailMedia_System`) へ上書き同期するルールは廃止（ルールから除外）** されました。
- 今後は、ローカルプレビューフォルダへのコピー・同期処理は行わないでください。

### 2. Gitプッシュおよび本番環境へのデプロイ【必須】
- すべてのコード修正完了後は、必ずGitへのコミット＆プッシュ（`git push origin main`）を行い、本番環境（AWS App Runner）へのデプロイを走らせて正常に完了したことを報告してください。

### 3. 利用サービス記憶と自動補完 (シームレスUX) 仕様
- 各ログイン画面に「ログイン状態と遷移を維持する」チェックボックス (`#remember-me` / `#remember`) を配置し、チェックON の 状態でログイン成功時のみ `last_active_service`（遷移先URL）、`last_email`（メールアドレス）、`last_password`（パスワード）を顧客のスマホ（LocalStorage）に保存します。
- ログアウト時でも、上記記憶データは削除せず維持し、次回起動時（`index.html` アクセス時）に自動的にそのログイン画面へリダイレクトされ、認証情報が自動入力された状態からスムーズに復帰（再ログイン）できる設計となっています。

---

## 🚨 必須ルール10: CORSブロック発生時の対応ルール (CORS Block Resolution)

CORSブロックの問題が発生した場合は、CORSポリシーで制限をかけるのではなく、速やかに以下の方針を適用して接続を確保してください。

1. **アクセス許可元（Origin）に `*`（すべてのドメイン）を指定する**
   CORSブロックの解消を最優先とするため、レスポンスヘッダーの `Access-Control-Allow-Origin` に `*` を指定して、すべてのドメインからのアクセスを許可する設定に変更してください。
2. **credentials が不要な場合、またはセッションをトークンなどで処理する場合**
   `Access-Control-Allow-Origin: *` と `Access-Control-Allow-Credentials: true` の同時返却はブラウザ仕様でエラーとなるため、CORSエラーを解決するために `credentials` の受け渡し（クッキー等）を解除するか、他の手段に代替した上で `*` による全許可を優先してください。

---

## 🚨 必須ルール11: AIルール直接永続化と一元化ルール (AI Rules Direct Persistence & Consolidation Policy)

今後、ユーザーから「ルール化して」または「ルールに追加して」という指示や要求があった場合は、AIは別の手段や返答だけで済ませず、必ず自動的かつ最優先で本ドキュメント（`C:\Users\one\.gemini\GEMINI.md`）にその内容を直接明文化して書き加え、永続化することを絶対ルールとします。

**【ルールファイルの `GEMINI.md` への一元化】**
**開発環境の `BACKEND_RULES.md` への書き込みおよび更新は不要とします。すべての開発規約、CSS優先度ルール、追加指示の明文化は、例外なく本ファイル（`C:\Users\one\.gemini\GEMINI.md`）のみに直接追記する形で一元管理してください。**

本ファイル（`GEMINI.md`）への追記および更新完了後は、速やかにGit（`origin main`）へのプッシュを実行してください。

---

## 🚨 必須ルール12: エンドツーエンド検証およびエラー報告時のデバッグログ強制追加ルール (End-to-End Verification & Debug Logging Policy)

AIは、API単体のテスト（Status 200が返ってくる等）のみで「通信成功・改修完了」と安易に判断してはなりません。また、ユーザーから「エラー」の報告や指摘があった場合、AI自身の推測だけでコードを修正して問題を解決しようとせず、**最優先で該当するサービス、コンポーネント、およびHTMLなどのフロントエンド処理（特にイベントハンドラやAPI呼び出しの最先頭・主要な分岐点）に対して、F12デベロッパーツールで追跡可能なデバッグ用ログ（`console.log`等）を直ちに追加すること**を絶対ルールとします。

実際のブラウザ上でのフリーズやCORSブロック、イベント未発火などのサイレントな不具合を100%確実に特定し、サイドエフェクトを防止するため、以下の「4つの検証チェックリスト」を厳守してください。

1. **エラー報告時のデバッグログ即時追加とコンソール確認**:
   - ユーザーから「ログインできない」「無反応」「エラーになる」等の報告があった場合、コードを書き換える前に、必ず関連する処理の最先頭（バリデーションや例外処理の前）に、何が起きているか（関数が動いたか、APIに送信したデータ、受信したデータ等）を表示する `console.log` を仕込み、デプロイしてブラウザ上のF12コンソール出力を最優先で確認・報告すること。
2. **CORSおよびミドルウェア定義順序の検証**:
   - `express.json` や `cors()` などの全体ミドルウェアが、すべてのルートハンドラ（`app.get`, `app.post`）よりも**上部に定義されていること**を確認し、部分的な適用漏れによるブラウザ側でのCORSブロックを完全に防止すること。
3. **JavaScriptモーダル (SweetAlert2等) の競合・フリーズ防止**:
   - 同一画面でモーダルやアラートを連続で開く処理を行う際は、必ず前のモーダルを閉じる処理（`Swal.close()` 等）を挟んでから次のモーダルを起動し、ライブラリ内部での描画デッドロック（フリーズ）を防ぐこと。
4. **フォーム送信・イベント発火 の 標準化**:
   - `setTimeout` / `iframe` 等のハックを廃止し、標準的な `event.preventDefault()` を用いた同期的な `fetch` フローで確実にJavaScript関数が発火する構造にすること。

---

## 🚨 必須ルール13: CSS優先度（z-index）の競合防止と設計基準

新規にCSSやHTML要素を追加・修正する際は、画面内のレイヤー（z-index）の競合による表示不具合を防ぐため、以下の優先度設計を厳守してください。

1. **z-index 設定時の競合防止と設計基準**
   - **通常のコンテンツ（Base）**: `auto` または `0` 〜 `9`
   - **コンテンツ内の一時的な浮遊要素（ツールチップなど）**: `10` 〜 `99`
   - **固定ヘッダー・フッターなどのレイアウト**: `100` 〜 `999`
   - **通常のモーダルダイアログ**: `1000` 〜 `4999`
   - **画面全体を覆うUI（ログインoverlay等）**: `5000` 〜 `9999` (既存の `#login-overlay` は `9999` に設定されています)
   - **サードパーティ・システム通知（SweetAlert2等）**: `10000` 以上 (今回の競合修正を元に `.swal2-container` は `10005` 以上に設定すること)
2. **検証義務**
   - 変更後はデベロッパーツールの「Network」や「Console」だけでなく、「Elements（要素）」パネルで実際のレンダリング状態を確認し、z-indexのバグでダイアログが背面に隠れていないかを必ず確認すること。

---

## 🚨 必須ルール14: AWS環境（S3/DynamoDB等）におけるSDKトランザクション安全対策ルール

AWS環境下でのSDKを利用したトランザクション（S3オブジェクトの入出力、DynamoDBへのクエリ・更新等）を記述する際は、ランタイムクラッシュおよび未定義例外（ReferenceError / TypeError）を100%防止するため、以下の基準を厳守してください。

1. **SDKクライアントインスタンスの明示的な初期化チェック**:
   - `docClient` (DynamoDB用) や `s3Client` (S3用) などのSDKクライアントオブジェクトを参照する際は、それらがプログラムの最上位レベルまたは実行前に確実にインスタンス化（`new S3Client()`, `DynamoDBDocumentClient.from()` 等）されていることを確認すること。
2. **グローバル環境変数 fallback 定数定義 of 徹底**:
   - SDKおよび連携API内で使用する環境変数（例：`AWS_S3_BUCKET_NAME` や `AWS_DEFAULT_REGION`）は、環境変数が未設定の場合（ローカル開発環境など）に備え、コード内で明示的なデフォルト値（フォールバック定数）を必ず定義すること。
   - 例: `const bucketName = process.env.AWS_S3_BUCKET_NAME || 'default-bucket'; const S3_BUCKET_NAME = bucketName; const AWS_REGION = process.env.AWS_DEFAULT_REGION || 'us-east-1';`
3. **トランザクション通信時の try-catch による例外捕捉とフォールバック処理**:
   - AWS SDKを呼び出す非同期通信箇所は、すべて `try-catch` ブロックで確実に保護し、万が一AWS側でエラーやアクセス拒否、ネットワークタイムアウトが発生した場合でも、サーバー自体をクラッシュ（500エラー）させずに代替手段（ローカルファイル保存、メモリ保存、適切なエラーレスポンス）に自動フォールバックさせること。

---

## 🚨 必須ルール15: データ登録時と参照・ログイン時のルール（正規化・マッピング）整合性担保ルール

システム改修や機能拡張に伴い、データ登録（インポート/登録）フローとデータ参照（ログイン/照会）フローの間でルールの不一致による整合性エラー（ReferenceError / 認証失敗など）が発生することを防止するため、以下の基準を厳守してください。

1. **マッピング・正規化関数の共通化と強制適用**:
   - ログイン時のロールの正規化（例: `getDatabaseRole` のように複数ロールを `store` にまとめる処理）や、特定のIDプレフィックス付与などのマッピング仕様がある場合、それを個別のファイルやエンドポイントごとに再実装せず、**必ず共通関数としてヘルパーモジュール等に抽出し、インポート時とログイン・参照時の両方で同一の関数を呼び出して処理を完結させること**。
2. **インポート/シードデータ投入時のバリデーションと前処理の統一**:
   - `database.json` などの初期データや外部インポートファイルを読み込んでデータベースに流し込む（同期する）処理を記述・改修する際は、参照側（APIやログイン側）が期待するデータ形式に完全に変換（前処理・正規化）してから保存すること。
   - 例: ロールが `advertiser` などのままDBに直接保存されてしまうと、ログイン側が `store` ロールで検索した際に一致しなくなるため、インポート時に `getDatabaseRole(role)` を確実に適用する。
3. **影響範囲チェックリストの更新とテスト実施**:
   - 既存のデータモデルに対する制限（バリデーションや形式の変更）や権限（ロールのマッピング）を改修した際は、必ず「データ登録側の処理」と「データ参照側の処理」の双方の関連ファイルを Grep し、影響を受ける箇所すべてに同一のロジックが適用されているか整合性確認テストを行うこと。
4. **データベース設計意図（主キー等）とプログラム処理の批判的検証**:
   - データベースのテーブルスキーマが持つ設計意図（例：`PRIMARY KEY (email, role)` による複合主キーでの役割ごとの分別管理）と、アプリケーションコード上の処理（例：ロールを一律 `store` にマッピングして丸め込む処理）に本質的な矛盾が生じていないか、必ず批判的に自己検証すること。
   - データベース側の設計メリットを打ち消すような、その場しのぎ of プログラム側マッピング規則を安易に導入せず、スキーマとロジックが一貫した設計となっていることを保証すること。

---

## 🚨 必須ルール16: 設計変更に伴う機能フロー全体および後続API連携の影響範囲トレース規約 (E2E Workflow & Parameter Dependency Tracing Policy)

データベースの主キー（一意性の定義）や認証モデル（ロールの分類など）の根本的な設計変更を行う際、変更されたパラメータが後続の処理フロー（特に二段階認証やデータ連携などの関連API）で確実に送信・使用されているかを検証し、送信漏れを防止するため、以下の基準を厳守してください。

1. **設計変更時の後続API・通信パラメータの連動チェック**:
   - ログイン時の役割（`role`）選択機能や複合主キー化など、主要なエントリーポイントのパラメータ仕様を変更した際は、ログインAPI単体だけでなく、**それに連動して実行される後続の関連API（2FA setup, enable, verify, reset 等）のすべてにおいて、新しい特定キー（`role` 等）が確実に送信・伝播されていること**をトレース確認すること。
2. **「一意の特定キー」に関する依存関係の検証**:
   - データベース上でレコードを一意に特定するためのキー構成（単一キーから複合キーへの変更など）を見直した場合は、システム全体のデータ参照処理（CRUD）に渡される引数が、新しい特定キーを過不足なく網羅しているか、自発的に批判的検証を行うこと。
3. **ブラウザ動作・通信フローのトレース**:
   - 変更があった部分に関して、ブラウザが最初にリクエストを発行してから、画面遷移し、二段階認証を経てダッシュボードに到達するまでの「機能フロー全体の動作シーケンス」を段階的にシミュレーションし、パラメータの渡し漏れ（`fetch` の body 内でのパラメータ欠落等）がないことをコードレビューで保証すること。

---

## 🚨 必須ルール17: 静的ファイル（HTML）のキャッシュ無効化と自動更新の適用

今後、システムに追加・修正を行うすべてのHTML配信について、デプロイ後にブラウザ側で古いファイルがキャッシュされ、ユーザーが手動でキャッシュクリアやスーパーリロードを行わなければならない手間を防止するため、以下の基準を厳守してください。

1. **静的HTMLファイルのCache-Controlヘッダー設定**:
   - `server_retail_dist.js` 等のサーバープログラムで静的ファイルを配信する際は、必ずHTMLファイル（拡張子が `.html`）に対して、ブラウザにキャッシュを保存させず、毎回必ずサーバーから最新 of HTMLを取得させるためのヘッダー（`Cache-Control: no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0`）を設定すること。
2. **新規HTMLおよび静的配信処理の適用**:
   - 今後新規に追加・変更するExpress等の配信ルーティングや静的配信フォルダの設定においても、このキャッシュ無効化設定が確実に適用されているか確認すること。

---

## 🚨 必須ルール18: AIエージェント駆動開発における行動・思考原則と推論時間確保ルール (AI Agent Development Principles & Thinking Policy)

AIは、ユーザーからのコード修正・機能追加の指示に対し、局所的な書き換えを行うだけでなく、システム全体の整合性と影響範囲を高度に推論して実行しなければならない。

### 1. 思考プロセス（コード生成前の必須ステップと30秒間の推論時間確保）
- コードの生成、ファイルの作成・編集、コマンド実行を行う前に、システム全体の整合性、依存関係、セキュリティ、リグレッションリスクを高度に検証するため、**最低30秒間**に相当する深い推論（思考プロセス）を内部で必ず実行し、その思考過程を出力（Thoughtまたは回答の冒頭）に明文化すること。
- 以下の3ステップを直ちに思考すること：
  1. **指示の意図理解**: ユーザーが本当に実現したいゴールは何か？
  2. **影響範囲（インパクト）の特定**:
     - データベース（スキーマ、JSON/SQLデータ構造, API連携）
     - 認証・認可フロー（Login, 2FA, セッション管理, ロール設定）
     - フロントエンド / バックエンドの整合性
  3. **リグレッション（先祖返り・破壊）リスクの抽出**: この修正によって、既存のどの機能にエラーやデバッグが発生し得るか？

### 2. 開発・改修における鉄則
- **「一貫性」の保持**: 
  あるコンポーネント（例: ログイン画面）に新しい概念（例: ロール、MFA要件）を導入する場合、必ず関連するすべての動線（例: 2FA画面、APIエンドポイントのバリデーション、DBクエリ）にも同様の対応が必要か検証し、必要な場合は同時に改修案を提示・適用すること。
- **構文・静的解析のエラー防止**:
  型定義、関数の引数、APIのレスポンスフォーマット、SQL/JSON の キー名に変更がある場合、依存するすべてのファイルを芋づる式に確認し、シンタックスエラーやコンパイルエラーを事前に排除すること。
- **セキュリティ・ファースト**:
  認証・認可（ロール設定）に関わる変更の場合、セキュリティホール（例: ログインは制限したが2FAをバイパスできる、APIがロールをチェックしていない等）が生まれないよう、防御的に設計すること。

### 3. 出力フォーマットの強制
ユーザーへの回答は常に以下の構成にすること。

- **① 影響分析レポート**
  - 本修正に伴う影響範囲のリスト（フロント、バック、DB、認証）
  - 見落とされがちな関連コンポーネント（例: 2FAへの影響の有無など）
- **② 変更計画**
  - 修正対象ファイル一覧と、それぞれの修正内容の要約
- **③ 実装コード**
  - 各ファイルの修正コード（差分または全体）
- **④ 修正後の検証・テスト観点**
  - デバッグや構文エラーを防ぐために、人間が確認すべき、またはAIがセルフチェックしたポイント

---

## 🚨 必須ルール19: 既存前提を否定する「逆説的スキーマ推論」と「ビジネス役割の存在意義」検証ルール (Paradoxical Schema Inference & Business Segments Validation Policy)

AIは、記述されているプログラムコードが「正しく動いているもの（仕様）」であるという現状肯定バイアスを完全に排除しなければならない。時間（秒数）をかけるだけの推論ではなく、以下の**「思考の切り口」**を義務付けることで、DB設計の意図やユーザーの「分別」の意図に能動的に気付くこと。

### 1. 「なぜ分けているのか」の存在意義の逆推論義務
コード内に、複数のビジネス概念（例: `advertiser`, `creator`, `agency`, `store`）を1つに丸めたり（例: すべて `store` にマッピングする）、条件分岐をバイパスして共通化している処理を発見した場合、AIは以下の問いを自分に立て、Thoughtにその回答を明文化しなければならない。
- **「もしすべてを1つに丸めてもシステムが成立するなら、なぜデータベース（SQLite/SQL）や画面（HTML）は、これらをわざわざ別の名前や別のテーブルで定義しているのか？」**
- **「分けることによるビジネス上の価値（権限, 売上管理, データの境界）は何か？」**
これを問い、「分けるべきものを開発者の実装都合で丸めている」と判断した場合は、既存のコードが「間違い（バグ）」であると看破し、丸め処理を廃止する修正を行うこと。

### 2. 「DBスキーマ最優先」と「プログラム疑い」の原則
- データベースの構造（カラム, 主キー, 複合主キー, テーブルリレーション）は、ビジネスモデルを具現化した「動かぬ事実（設計意図）」である。
- アプリケーションコード（JS/API）に記述されている処理が、DBスキーマが表現している「分別（セグメンテーション）」を無視または上書きしている場合、**「プログラム側が間違っており、DBスキーマが正しい」**という仮説を最優先で立てて検証すること。

### 3. 「同一主キー（Email等）・複数ロール」のユースケース自動認識
- 認証やアカウント管理において、メールアドレスが同一（または重複可能）で、ロール（役割）が異なるレコードがDBに定義されている場合、それは**「同一の人間（メールアドレス）が、文脈や画面に応じて複数の役割（広告主, クリエイター等）を使い分ける」**というマルチテナント・マルチロールの意図であると自動的に認識すること。
- これを一律のロールに集約するいかなる処理も、ユーザーの「データ分別」の意図を破壊するバグであると定義する。

---

## 🚨 必須ルール20: 広告主・クリエイター等の動画配信における厳格なAIコンテンツ審査と審査スキップの絶対禁止ルール (Strict Video Moderation & Bypass Prohibition Policy)

広告主（Advertiser）およびクリエイター（Creator）がアップロード・配信する動画コンテンツのAI審査プロセスにおいて、いかなる場合でも安易な開発都合や一時的なネットワーク制限を理由とした「審査チェックのスキップ」「自動通過」「スルー処理」の実装を絶対禁止とします。

1. **実映像データ（バイナリ）に基づく解析の義務付け**:
   - ローカルファイルアップロードか、YouTube等の外部リンク形式かに関わらず、必ず実際の映像データ（バイナリやストリーム情報）をAI（Gemini等）に入力し、暴力・詐欺・誇大広告・規約違反等の映像内コンテンツを厳密に画像・音声解析すること。タイトルなどのテキストメタデータのみの簡易審査ですり抜けさせることを厳格に禁止する。
2. **システム制限・通信失敗時のフォールバック（Heuristics 審査）ルール**:
   - 映像検証が実行できない一般的なシステムエラーでは即時「配信不可（アップロード拒否）」とするが、**YouTubeリンク等のダウンロードアクセス制限（Bot対策等）が原因で直接の映像検証が不可能な場合に限り、メタデータ（タイトルやキャンペーン名）に対する Heuristics 審査（「詐欺」「ウイルス」「警告」「簡単に稼げる」「即日融資」「お試し無料」の有無チェック）を行い、ポリシー違反キーワードを含まなければ自動で承認（status: 'active' または safe: true）を許可する。**
   - 安全性が100%確認できない動画はシステム上に登録・配信させないフェイルセーフを大前提としつつ、YouTubeダウンロード制限等の外部API制限下においては、Heuristicsポリシーチェックが合格した動画のみを正常配信可能にする。

---

## 🚨 必須ルール21: 外部・内部ライブラリのグローバル登録仕様および動作要件の事前検証義務 (Library Integration & Global Scope Verification Policy)

AIは、外部ライブラリ（CDN、npm等）や内部共通モジュールを追加・更新・参照する際、単にURLやインポートパスが正しい（404にならない等）ことのみを確認して作業を完了してはならない。必ず「ロード完了後、そのライブラリが実行環境（ブラウザのグローバルオブジェクト/window、またはNode.js of global空間）に期待通りに公開・登録される仕様になっているか」を事前に公式ドキュメントや仕様から検証し、それに合わせたインポート方式（グローバル変数参照、ESM of 動的インポート `import()`、CommonJSの `require` 等）を正しく選定して実装することを義務付ける。
表面的なパス修正だけを行い、動作仕様の確認不足によるエラー再発（手戻り）を引き起こすことを厳格に禁止する。

---

## 🚨 必須ルール22: ビジネスモデルの理解とゴールラインの定義（Business Model Integration & Goal Alignment Policy）

開発を進める際、AIは常に本システムのビジネス構造と「なぜこのシステムを構築しているのか」という目的（ゴールライン）を意識し、ビジネスメリットを最大化する設計を選択すること。

### 1. 共通のビジネスモデル構造と市場優位性
本システムは「小売り店舗のサイネージ広告を共同運用する、誰もが参加可能なオープン型プラットフォーム（YouTube型モデル）」である。他社がクローズドな「自社専用サイネージ枠」しか展開していない中で、本システムは店舗・広告主・クリエイター等の全ステークホルダーを巻き込んだYouTube型の共同運用モデルを構築し、新しい巨大市場の可能性を切り拓く。

- **小売店（店舗）**:
  - 店頭サイネージの設置において、サーバー代等のシステム維持費は「必要経費」として売上から処理され、経費を差し引いた広告収益の50%が分配（支払）される。
  - 店頭には多種多様なメーカーの商品が日常的に並んでおり、広告主（メーカー等）が購入接点でダイレクトに広告を出すビジネスメリットが極めて高い。
  - 店舗運営に役立つ各種のダッシュボード・管理サービスは小売店側には「基本無料」で提供し、参加するメリットを最大化する。
- **広告主**:
  - 購買接点である店舗サイネージへ手軽に広告を出稿でき、その成果としてPOSデータや顧客行動データの分析レポートをリテアド経由で回収できる。
- **クリエイター**:
  - 各広告キャンペーンに最適化された動画コンテンツを自由に制作・提供する。
  - 動画のクオリティを高めて広告効果を向上させることで、自身の収益が増大する成果報酬・評価エコシステムを形成する。
- **代理店**:
  - 新たに開拓されるサイネージ共同運用市場において、既存のマス広告やネット広告と同等水準の確実な手数料（マージン）を獲得する。

### 2. 開発への適用原則（開発 of ゴールライン）
- **参加障壁の低減とプラットフォームの開放性**: 「誰もが参加できるYouTube型プラットフォーム」として、店舗・広告主・クリエイターの登録やデバイス設定（Android/Windows）を徹底的に省力化・自動化する開発を最優先する。
- **経費計算・分配ロジックの正確性**: サーバー代をはじめとする必要経費の計上処理と、小売店への50%分配、および各種決済トランザクションの処理・永続化（`saveFinanceDB` 等）の整合性を厳格に保つ。
- **購買分析・データ還元の最大化**: 多数の商品を扱う小売店において、広告主がPOSデータや顧客行動の相関分析をシームレスに行えるAPI・レポーティング機能を設計する。
- **ネットワーク効果の最大化**: 参加者が増えるほど広告効果とデータの精度が自己増殖的に高まるため、大量 of デバイス接続やデータトラフィックのスケールに対応可能な非同期設計（SQLite移行等）を厳守する。

---

## 🚨 必須ルール23: クリエイター個別管理と配信ステータス表示の徹底 (Creator Multi-tenant Isolation & Status Visibility Policy)

1. **認証に基づくデータの独立性（マルチテナント隔離）**:
   - クリエイターの動画アップロードや統計情報の取得API（`/api/creator/upload`, `/api/creator/stats` 等）では、必ずログイン中のJWT認証（Cookieから取得したメールアドレス）を基準として動画の登録・フィルタリングを行うこと。グローバルな配列から全クリエイターの動画をそのまま返却することは厳禁とし、各クリエイターが自分の登録した動画および統計情報（views, revenue）のみを独立して管理できる構造を保証すること。
2. **詳細な配信中情報の確認（ステータス可視化）**:
   - 各クリエイターが「何の動画が」「どこで」「いつ」配信されているかをダッシュボード上で視覚的に確認できるよう、動画リストカードに「配信ターゲット店舗（STORE-001等）」「配信枠（PAID等のコラボ枠）」「配信スケジュール」などの詳細なステータス情報を明確に表示すること。
