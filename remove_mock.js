const fs = require('fs'); let c = fs.readFileSync('shift_manager.html', 'utf8'); const idx1 = c.indexOf('const mockNames'); const idx2 = c.indexOf('saveStaffDB();', idx1); if (idx1 > -1 && idx2 > -1) { c = c.substring(0, idx1) + c.substring(idx2); fs.writeFileSync('shift_manager.html', c); console.log('Mock staff generation removed successfully!'); } else { console.log('Tags not found.'); }
